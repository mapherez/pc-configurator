# Local development (SQLite)
DATABASE_URL="file:./dev.db"

# Turso (libSQL over HTTP) - optional
# TURSO_DATABASE_URL=libsql://your-host.turso.io
# TURSO_AUTH_TOKEN=your-token

PORT=3001
# Comma-separated list; can include regex via deploy-time config only.
CORS_ORIGINS=http://localhost:5173,https://*.vercel.app,https://app.yourdomain.com
